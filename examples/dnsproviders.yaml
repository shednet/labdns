apiVersion: labdns.shednet.dev/v1alpha1
kind: DNSProvider
metadata:
  name: www
spec:
  zones:
  - name: example.com
  ipSources:
    ipv4:
      nodeLabel: networking.example.com/public-ipv4
    ipv6:
      # Values use v6- plus canonical IPv6 text with ':' replaced by '-'.
      nodeLabel: networking.example.com/public-ipv6
  recordDefaults:
    ttl: 300
    deletionDelay: 60s
  providerSpecific:
    defaults:
    - name: external-dns.alpha.kubernetes.io/cloudflare-proxied
      value: "false"
    annotationKeys:
    - external-dns.alpha.kubernetes.io/cloudflare-proxied
---
apiVersion: labdns.shednet.dev/v1alpha1
kind: DNSProvider
metadata:
  name: vpn
spec:
  zones:
  - name: example.com
  ipSources:
    ipv4:
      nodeLabel: networking.example.com/vpn-ipv4
    ipv6:
      # For example, 2001:db8:2::10 is stored as v6-2001-db8-2--10.
      nodeLabel: networking.example.com/vpn-ipv6
  recordDefaults:
    ttl: 300
    deletionDelay: 60s
